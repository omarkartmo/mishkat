# دليل تثبيت وتشغيل نظام المشكاة للمكتبات المدرسية (نسخة سطح المكتب)

---

## 1. الإجابة عن سؤالك: هل هو نفس البرنامج؟
**نعم، هو نفس النظام البرمجي بالكامل**، ولكن طريقة التشغيل تكون كالتالي:

1. **الحاسوب المركزي (حاسوب أمين المكتبة):**
   - يُشغل كـ **"الخادم الرئيسي والمستودع المركزي"** (يحتفظ بجميع قواعد البيانات، الفهارس، وسجلات الطلبة والإعارات).
   - يتم تشغيل الخادم بالأمر: `npm run dev` أو `npm start`.

2. **الحواسيب المحمولة (أجهزة الطلبة):**
   - يُشغل عليها التطبيق في وضع **النافذة المكتبية المقفلة والمضبوطة (Controlled Desktop App)**.
   - يتصل تلقائياً وبشكل فوري بالحاسوب المركزي لأمين المكتبة عبر الشبكة المحلية (LAN/Wi-Fi).

---

## 2. طرق الحصول على ملف تشغيل سطح المكتب (`mishkat.exe` / المشكّل المكتبي):

### الطريقة الأولى: إنشاء ملف التطبيق التنفيذي المستقل `mishkat.exe` (Electron)
تم تضمين ملف التكوين `electron-main.cjs` المخصص للتحكم بالأمان. لتحزيمه في ملف تثبيت واحد `.exe`:

1. قم بتصدير المشروع (Export to ZIP) من قائمة الإعدادات في أعلى المنصة.
2. افتح مجلد المشروع على جهاز الحاسوب ونفذ الأوامر التالية:
```bash
npm install --save-dev electron electron-builder
npx electron-builder --win
```
3. سينتج لك ملف تنفيذي جاهز للتثبيت:
📁 `dist/mishkat Setup.exe` أو `dist/mishkat.exe`

---

### الطريقة الثانية: التشغيل المكتبي الفوري بنقرة واحدة (بدون برامج إضافية)
تم إرفاق ملف التشغيل المكتبي `launch-mishkat-app.bat`:

1. **على حاسوب أمين المكتبة:**
   - افتح موجه الأوامر (CMD) واكتب `ipconfig` لمعرفة الـ IP (مثال: `192.168.1.15`).

2. **على حواسيب الطلبة:**
   - ضع ملف نصي صغير اسمه `server_ip.txt` وبداخله عنوان الـ IP فقط: `192.168.1.15`.
   - انقر مرتين على ملف `launch-mishkat-app.bat` (أو اصنع له اختصاراً باسم "نظام المشكاة").
   - سيفتح التطبيق في **نافذة سطح مكتب كاملة ومستقلة تماماً**، بدون شريط روابط وبدون إمكانية تصفح الإنترنت العام.
