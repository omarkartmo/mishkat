# دليل تثبيت وتشغيل نظام المشكاة للمكتبات المدرسية (نسخة سطح المكتب والشبكة المحلية)

---

## 1. بنية تشغيل النظام في المدرسة

يعمل نظام المشكاة بنمط الخادم والعميل (Client-Server Architecture) عبر الشبكة المحلية للمدرسة (LAN):

1. **الحاسوب المركزي (حاسوب أمين المكتبة - Master Server):**
   - يُشغل كـ **"الخادم الرئيسي والمستودع المركزي"** (يحتفظ بقواعد البيانات المركزية، سجلات الفهارس والكتب، ملفات PDF/EPUB، وحسابات الطلبة والإعارات).
   - يتم تشغيل خادم الإنتاج إما كخدمة ويندوز دائمة عبر **`Install-Mishkat-Service.bat`**، أو يدوياً عبر **`Mishkat-Server.bat`** (أو عبر الأمر `npm start`).
   - المنفذ الافتراضي هو `3000` (قابل للتعديل عبر متغير `PORT` في ملف `.env`).

2. **أجهزة الطلبة والمطالعة (Student Reading Stations):**
   - تتصل بالخادم المركزي عبر الشبكة المحلية (LAN أو Wi-Fi).
   - يمكن فتح النظام عبر أي متصفح ويب بزيارة رابط الخادم:
     `http://<SERVER_IP>:3000`
    - أو تشغيله عبر **تطبيق الطالب المكتبي الرسمي (MISHKAT Student - Tauri 2 + WebView2)** الذي يوفره المثبت الموحد `MISHKAT-Setup.exe`.

---

## 2. طرق تشغيل واجهة سطح المكتب على أجهزة الطلبة:

### الطريقة المعتمدة: التثبيت الموحد (MISHKAT-Setup.exe)
1. شغل مثبت النظام الموحد **`MISHKAT-Setup.exe`**.
2. اختر دور الجهاز: **`MISHKAT Student (محطة الطالب والباحث)`**.
3. يقوم المثبت تلقائياً بإنشاء اختصار سطح المكتب لتطبيق الطالب `mishkat-student.exe` المعتمد على **Tauri 2** و **WebView2**، مع توجيه آمن نحو الخادم المركزي عبر الشبكة المحلية (LAN).
4. يعمل التطبيق في بيئة معزولة ومحمية تمنع القوائم المنسدلة الخارجية وتحصر البحث والمطالعة في موارد المكتبة والبوابات المعرفية المعتمدة.

---

## 3. الأمان وجدار الحماية وسجلات التشغيل

- **جدار الحماية (Firewall):** افتح المنفذ 3000 (TCP) في نطاق الشبكة الخاصة فقط عبر:
  ```cmd
  netsh advfirewall firewall add rule name="Mishkat Library" dir=in action=allow protocol=TCP localport=3000 profile=private
  ```
- **سجلات التشغيل:** تسجل كافة أحداث الخادم في: `LibraryData\logs\mishkat.log`.
- **النسخ الاحتياطي والاسترجاع الذري:** متاح بالكامل عبر لوحة تحكم أمين المكتبة (إعدادات النظام) أو واجهة الـ API (`/api/v1/backups/create` للتصدير، و`/api/v1/backups/:fileName/restore` للاسترجاع الذري التلقائي مع نسخة أمان مسبقة).
