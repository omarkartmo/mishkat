# تحديث نظام المشكاة للمكتبات بضغطة زر وتثبيت الاختصار (One-Click Update & Shortcut Guide)

---

## 1. كيف تقوم بتحميل وتثبيت البرنامج على حاسوبك؟

1. **تحميل المشروع كملف مضغوط (ZIP):**
   - من القائمة العلوية لمنصة Google AI Studio، اضغط على زر **Settings (الإعدادات)** أو أيقونة التحميل ثم اختر **Export as ZIP** أو **Download ZIP**.
   - فك الضغط عن الملف في مجلد على حاسوبك (مثلاً: `C:\MishkatLibrary`).

2. **التثبيت والتشغيل لأول مرة:**
   - افتح المجلد واضغط نقرة مزدوجة على:
     - **`Mishkat-Server.bat`** (إذا كان هذا حاسوب أمين المكتبة المركزي).
     - **`Mishkat-Client.bat`** (إذا كان هذا حاسوب طالب/محطة مطالعة).
   - أو إذا قمت بتحزيمه كـ `.exe` باستخدام `npx electron-builder`:
     - سيظهر لك مباشرة **معالج التثبيت الذكي (Setup Wizard)** ليطلب منك فقط اختيار (Server أو Client).
     - يحتوي المعالج على خيار تلقائي مفعل: `[✔] إنشاء أيقونة واختصار للمكتبة على سطح المكتب (Desktop Shortcut)`.

---

## 2. إنشاء وتثبيت اختصار سطح المكتب تلقائياً (Desktop Shortcut):

تم تضمين ملف مخصص يقوم بإنشاء الاختصار على سطح المكتب فوراً:

### 📌 الطريقة التلقائية:
عند تشغيل ملف **`Create-Desktop-Shortcut.bat`** (أو من خلال معالج التثبيت)، يقوم النظام تلقائياً بإنشاء اختصار رسمي باسم **"نظام المشكاة للمكتبة"** على سطح المكتب مع أيقونة المكتبة الرسمية، بحيث يفتح النظام فوراً بنقرة واحدة دون الحاجة للدخول للمجلدات أو فتح سطر الأوامر.

---

## 3. آلية تحديث البرنامج بضغطة زر (One-Click Update Mechanism):

لتحديث نسختك على الحاسوب بكل ما يتم تطويره وتحسينه هنا دون فقدان بياناتك المحلية:

### 🚀 سكربت التحديث السريع بنقرة واحدة (`Update-Mishkat.bat`):
تم تجهيز ملف `Update-Mishkat.bat` المرفق مع المشروع، وعند النقر عليه:
1. يقوم بحفظ نسخة احتياطية من بياناتك ومستنداتك الحالية في مجلد `backups/`.
2. يجلب أحدث الملفات والتحسينات البرمجية تلقائياً.
3. يقوم بتثبيت أي مكتبات أو تحديثات جديدة.
4. يعيد تشغيل الخادم والبرنامج فوراً بالنسخة المحسنة.

### 🌐 التحديث عبر Git (للمزامنة الفورية والتلقائية):
إذا قمت بربط المشروع بمستودع GitHub (عبر خيار Export to GitHub في المنصة):
- يكفيك فقط الضغط على ملف `Update-Mishkat.bat` ليقوم بتنفيذ `git pull` وتطبيق كافة التحديثات في ثوانٍ معدودة.

---

## 4. التصميم المتجاوب الجديد (Responsive Design):

تم تحديث واجهة النظام بالكامل لتصبح متوافقة بنسبة 100% مع:
- **شاشات الحواسيب المكتبية والمحمولة** (Desktop & Laptops).
- **الأجهزة اللوحية (Tablets/iPads)** مع قائمة منبثقة تفاعلية (Drawer Navigation).
- **الهواتف النقالة (Smartphones / iOS & Android)** عبر المتصفح برابط محلي مباشر، مع قائمة جانبية ذكية (Hamburger Menu)، وشريط بحث سلس، وتوافق تام لأحجام الأزرار والبطاقات (Touch Target 44px+).
