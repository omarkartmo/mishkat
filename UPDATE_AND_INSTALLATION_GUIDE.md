# دليل تثبيت وتشغيل وإدارة نظام المشكاة للمكتبات المدرسية (بيئة الإنتاج والشبكة المحلية)
# Mishkat School Library — Production Deployment, Operations & Maintenance Guide

---

## 1. المتطلبات الأساسية للنظام (System Prerequisites)

لتشغيل نظام المشكاة كخادم مركزي للمكتبة المدرسية على حاسوب يعمل بنظام Windows (Windows 10, 11, أو Windows Server):

1. **بيئة تشغيل Node.js:**
   - الإصدار المدعوم: **Node.js 20 LTS أو Node.js 22 LTS** (يوصى بـ Node 22).
   - التحقق من التثبيت في موجه الأوامر (CMD):
     ```cmd
     node -v
     npm -v
     ```
2. **صلاحيات الإدارة (Administrator Rights):** مطلوبة فقط عند تثبيت خدمة ويندوز وضبط جدار الحماية (Firewall).
3. **أداة NSSM (اختياري، للتشغيل كخدمة ويندوز ذاتية الإقلاع):**
   - تحميل مجاني من: `https://nssm.cc/download`
   - وضع ملف `nssm.exe` في مجلد المشروع أو إضافته إلى متغيرات مسار النظام (`PATH`).

---

## 2. خطوات التثبيت والتشغيل لأول مرة (First-Time Setup)

### الخطوة 1: استخراج المشروع وضبط ملف الإعدادات
1. ضع مجلد المشروع في مسار ثابت على الخادم (مثال: `C:\MishkatLibrary`).
2. قم بنسخ ملف `.env.example` إلى `.env`:
   ```cmd
   copy .env.example .env
   ```
3. افتح ملف `.env` وقم بإنشاء وتعيين مفتاح تشفير أمني عشوائي قوي (`JWT_SECRET`) يتكون من 32 حرفاً على الأقل:
   - لتوليد مفتاح آمن بسرعة عبر سطر الأوامر:
     ```cmd
     node -e "console.log(require('crypto').randomBytes(48).toString('hex'))"
     ```
   - الصق المفتاح في متغير:
     ```env
     JWT_SECRET=your_generated_secret_key_here_at_least_32_characters
     ```
4. **تحديد المنفذ (PORT):**
   - افتراضياً يعمل النظام على المنفذ `3000`.
   - إذا كان المنفذ 3000 مشغولاً، يمكنك تغييره بسهولة عبر ملف `.env`:
     ```env
     PORT=8080
     ```

### الخطوة 2: تثبيت التبعيات وبناء ملفات الإنتاج (Production Build)
افتح موجه الأوامر (CMD) في مجلد المشروع ونفذ:
```cmd
npm ci
npm run build
```
- سينتج مجلد `dist/` يحتوي على الملفات المترجمة والمجمعة لتطبيق React وخادم Express (`dist/server.cjs`).

### الخطوة 3: ضبط جدار حماية ويندوز (Windows Firewall)
لكي تتمكن أجهزة الطلبة والهواتف من الدخول إلى الخادم عبر الشبكة المحلية (LAN):
- افتح موجه الأوامر كمسؤول (Run as Administrator) ونفذ الأمر التالي لفتح المنفذ (3000 أو المنفذ المحدد):
  ```cmd
  netsh advfirewall firewall add rule name="Mishkat School Library" dir=in action=allow protocol=TCP localport=3000 profile=private
  ```
- أو عبر PowerShell كمسؤول:
  ```powershell
  New-NetFirewallRule -DisplayName "Mishkat School Library (Port 3000)" -Direction Inbound -LocalPort 3000 -Protocol TCP -Action Allow -Profile Private,Domain
  ```
> **تنبيه أمني:** تم حصر القاعدة في النطاق المحلي والخاص (`profile=private,domain`) لمنع التعرض غير المقصود لشبكات الإنترنت العامة.

---

## 3. خيارات تشغيل الخادم المركزي (Running the Server)

### الخيار أ: التشغيل كخدمة ويندوز تلقائية التشغيل (موصى به لبيئة الإنتاج)
يوفر هذا الخيار تشغيل النظام تلقائياً في الخلفية بمجرد تشغيل الحاسوب، مع إعادة التشغيل التلقائي عند أي توقف:
1. تأكد من وجود أداة `nssm.exe` في مجلد المشروع أو مسار النظام.
2. انقر بزر الفأرة الأيمن على ملف **`Install-Mishkat-Service.bat`** واختَر **"تشغيل كمسؤول" (Run as Administrator)**.
3. ستقوم الأداة بتهيئة الخدمة باسم `MishkatLibraryService` وضبط إقلاعها التلقائي (`SERVICE_AUTO_START`).
4. للتحقق من حالة الخدمة في أي وقت:
   ```cmd
   nssm status MishkatLibraryService
   ```
5. لإلغاء تثبيت الخدمة: انقر كمسؤول على **`Uninstall-Mishkat-Service.bat`**.

### الخيار ب: التشغيل اليدوي المباشر عبر الواجهة (Foreground Launcher)
1. انقر نقراً مزدوجاً على ملف **`Mishkat-Server.bat`**.
2. يقوم الملف بفحص البيئة وتثبيت ملفات الإنتاج وتشغيل خادم الإنتاج الفعلي (`node dist/server.cjs`).
3. يظهر لك عنوان IP المحلي للدخول من الهواتف وأجهزة الطلبة.
4. **تنبيه:** لا تغلق النافذة أثناء فترة دوام المكتبة في هذا النمط.

---

## 4. فحص الصحة والسلامة التشغيلية (Health Monitoring)

يوفر خادم المشكاة نقطة فحص صحة تشغيلية خفيفة بدون مصادقة لمراقبة النظام:
- الرابط: `http://localhost:3000/api/v1/health`
- القيم المرجعة:
  - `status: "healthy"`: الخادم يعمل، وقاعدة البيانات متصلة، ومجلد التخزين قابل للقراءة والكتابة.
  - `status: "unhealthy"` (رمز HTTP 503): في حال تعطل قاعدة البيانات أو تعذر الوصول إلى وسائط التخزين.

---

## 5. إدارة السجلات (Production Logging)

- تُحفظ جميع سجلات العمليات وأخطاء الخادم في:
  ```
  LibraryData\logs\mishkat.log
  ```
- **آلية تدوير السجلات (Log Rotation):**
  - الحد الأقصى لحجم ملف السجل: **5 ميجابايت**.
  - عند امتلاء الملف، يتم تدويره والاحتفاظ بما يصل إلى **3 ملفات أرشيفية** (`mishkat.1.log`، `mishkat.2.log`، إلخ).
- **حماية الخصوصية والأمان:**
  - يتم حجب كلمات المرور ورموز التحقق (JWT tokens) وتفاصيل الاعتماد الحساسة تلقائياً برموز `[REDACTED]`.

---

## 6. إجراءات التحديث الدوري والترقية الآمنة (Production Update Procedure)

عند صدور تحديث جديد للنظام، اتبع الخطوات التشغيلية التالية لضمان سلامة البيانات:

### الخطوة 1: أخذ نسخة احتياطية أولاً (Backup First)
- من شاشة لوحة تحكم أمين المكتبة، اذهب إلى "النسخ الاحتياطي" واضغط **"إنشاء نسخة احتياطية فورية"**.
- تُحفظ النسخة في مجلد: `LibraryData\backups\`.
- *(ملاحظة هامة: آلية الاسترجاع التلقائي لقاعدة البيانات Restore قيد التطوير في المرحلة 15.3، والاحتفاظ بملف النسخة الاحتياطية في مكان آمن يضمن سلامة البيانات).*

### الخطوة 2: تطبيق التحديث عبر السكربت المخصص
- قم بتشغيل ملف **`Update-Mishkat.bat`**:
  1. يقوم السكربت بإيقاف خدمة ويندوز مؤقتاً لتفادي قفل الملفات.
  2. يجلب الشيفرة المحدثة عبر `git pull`.
  3. يثبت أي حزم جديدة عبر `npm ci`.
  4. **يعيد تجميع وبناء ملفات الإنتاج المحدثة عبر `npm run build`**.
  5. يعيد تشغيل خدمة ويندوز تلقائياً.
- بعد اكتمال التحديث، افتح رابط الفحص `http://localhost:3000/api/v1/health` للتأكد من سلامة النظام.

---

## 7. الدخول من أجهزة الطلبة عبر الشبكة المحلية (LAN Access)

1. تأكد من اتصال حاسوب الطالب بنفس شبكة المدرسة (LAN / Wi-Fi).
2. افتح متصفح الإنترنت (Edge، Chrome، Safari) واكتب عنوان IP الخاص بالحاسوب المركزي مع المنفذ:
   ```text
   http://192.168.x.x:3000
   ```
3. سيعمل التطبيق بكامل وظائفه مع واجهة متجاوبة وسريعة لكافة الشاشات.
